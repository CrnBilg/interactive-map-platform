# Optional, only needed for Google Maps Embed Street View in the 360 modal.
# The default map still uses Leaflet + OpenStreetMap.
VITE_GOOGLE_MAPS_EMBED_API_KEY=your_google_maps_embed_api_key_here

# Optional. Backend Socket.IO URL for local development.
VITE_SOCKET_URL=http://localhost:5000
